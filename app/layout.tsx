import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "EmiratesAnalytics.me - Data-Driven Decisions for the Emirates",
  description:
    "Empower your UAE business with local insights and smart dashboards. Built specifically for Emirates enterprises with cultural understanding and regulatory compliance.",
  keywords: "UAE analytics, Emirates business intelligence, Dubai data analytics, Abu Dhabi business insights",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
